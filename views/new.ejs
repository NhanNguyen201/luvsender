<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/x-icon" href="/heart.jpg">
    <meta name="description" content='<%= locals.quote %>'>

    <title>Send to your luv ! 😝</title>
    <meta itemprop="name" content="Send to your luv ! 😝">
    <meta itemprop="image" content="./heart.jpg">
    <!-- fb -->
    <meta property="og:type" content="Website">
    <meta property="og:title" content="Send to your luv ! 😝">
    <meta property="og:image" content="./heart.jpg">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="./style.css">
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col col-xl-6 col-lg-6 col-md-8 col-sm-10 col-xs-10 cenCol">
                <h2 class="font-weight-light text-center">Send to your love !😝😝😝</h2>
                <% if (locals.host && locals.linkResult) { %>
                    <% if (locals.quote) { %>
                        <i><%= locals.quote %></i>
                    <% } %>
                    <% for(let i=0; i< locals.linkResult.length; i++) {%>
                        <div class="m-10">
                            <input type="text" value="<%= locals.linkResult[i].host %>/<%= locals.linkResult[i].type %>?_id=<%= locals.linkResult[i].linkId %>" readonly class="form-control"/>
                            <div class="btn-container">
                                <button class="btn btn-primary copy-btn">Copy</button>
                                <a href="/<%= locals.linkResult[i].type %>?_id=<%= locals.linkResult[i].linkId %>" class="btn btn-primary copy-btn" role="button">Go to the link</a>
                            </div>
                        </div>
                    <% } %>
                <% }  else { %>
                    <p>There is nothing here</p>
                <% } %>    
            </div>
        </div>
    </div>
    <script type='text/javascript'>
        if(document.querySelectorAll(".copy_btn")) {
            const copyBtns = document.querySelectorAll(".copy_btn")
            copyBtns.forEach(btn => {
                btn.onclick = e => {
                    console.log("previous value: ", btn.parentNode.previousSibling.value)
                }
            })
        }
    </script>
</body>
</html>